-- ============================================================================
--  V8 — заказы можно оформлять без CRM (создавать «с нуля» в приложении):
--  lead_id становится необязательным. UNIQUE остаётся (в Postgres допускает
--  несколько NULL). Идемпотентно: DROP NOT NULL на уже nullable — no-op.
-- ============================================================================

ALTER TABLE orders ALTER COLUMN lead_id DROP NOT NULL;
