-- ============================================================================
--  V17 — UTM-метки в транзакции не храним (аналитика — на стороне
--  веб-аналитики); колонки добавлены уже применённой V15, убираем.
-- ============================================================================

ALTER TABLE payment_transaction DROP COLUMN utm_source;
ALTER TABLE payment_transaction DROP COLUMN utm_medium;
