-- Маркетплейс-заказы — розница по определению, включая брошенные корзины
-- (retail теперь ставится при создании заказа, а не при оплате).
-- Неоплаченные/отменённые отсекаются фильтром по payment_status в запросах.
UPDATE orders SET retail = TRUE WHERE public_id IS NOT NULL;
