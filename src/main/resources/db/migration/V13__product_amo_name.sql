-- ============================================================================
--  V13 — имя товара в каталоге АМО рядом с его id.
--    product.amo_product_name — сохраняется при выборе товара в админке из списка АМО,
--    чтобы позиции заказа (order_items.product_name) назывались так же, как в АМО.
-- ============================================================================

ALTER TABLE product
    ADD COLUMN amo_product_name VARCHAR(255);
