-- Порядок фотографий товара, заданный в админке: имена файлов из S3-папки товара
-- в нужном порядке, по одному на строку. Файлы, которых здесь нет (загружены позже),
-- показываются после перечисленных. Сами файлы остаются в S3 — это только порядок.
ALTER TABLE product
    ADD COLUMN photo_order TEXT;
